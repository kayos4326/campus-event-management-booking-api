# Local dev (npm run dev) hits the real deployed backend directly, since there's no
# separate local backend + DB running normally. Production build doesn't need this —
# api.js falls back to the relative /events/api path, correct once served same-origin.
VITE_API_BASE=https://chaotic-hell.eastasia.cloudapp.azure.com/events/api
